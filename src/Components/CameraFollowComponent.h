#ifndef CAMERAFOLLOWCOMPONENT_H
#define CAMERAFOLLOWCOMPONENT_H

struct CameraFollowComponent {
    // No data is needed to be stored, so we can have a default constructor
    CameraFollowComponent() = default;
};


#endif